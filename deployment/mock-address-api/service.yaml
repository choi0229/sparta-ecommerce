apiVersion: v1
kind: Service
metadata:
  name: mock-address-api-svc
  namespace: ecommerce
spec:
  selector:
    app: mock-address-api
  ports:
    - protocol: TCP
      port: 8090
      targetPort: 8080
